import React from 'react';
import { Container } from 'semantic-ui-react';
import LoginForm from '../modules/authorize/components';

const LoginPage = () => (
  <Container>
    <LoginForm />
  </Container>
);

export default LoginPage;
