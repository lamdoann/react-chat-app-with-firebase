import React from 'react';
import LoginButton from '../modules/authorize/components/LoginButton';

const Login = () => (
  <div className="chat-login">
    <LoginButton />
  </div>
);

export default Login;